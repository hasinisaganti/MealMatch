# MealMatch

AI-assisted meal subscriptions for students, powered by verified local home cooks.

## Local setup

```powershell
npm.cmd install
npm.cmd install --prefix server
npm.cmd install --prefix client
npm.cmd run seed --prefix server
npm.cmd run dev
```

Open `http://localhost:5173`. Demo: `student@mealmatch.test` / `demo123`.
